// =============================================================================
// SETTINGS — Global app settings
// =============================================================================
(function () {
  'use strict';
  window.LP = window.LP || {};
  window.LP.Settings = {
    numberDisplay: 'fraction',   // 'fraction' | 'decimal'
    detailLevel:   'short',      // 'short' | 'long'
    language:      'mn',
  };
})();
